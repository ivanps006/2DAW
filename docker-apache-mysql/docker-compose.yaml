version: "3.8"

services:
  web:
    image: php:8.2-apache
    container_name: apache_web
    restart: unless-stopped
    ports:
      - "8080:80"
    volumes:
      - ../DAES/ActividadesPrimerasPHP:/var/www/html
    networks:
      - app_network

networks:
  app_network:
    driver: bridge